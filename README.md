# Data folder

Place the frozen GOLD modeling dataset here:

```text
Unified_DES_dataset_GOLD_descriptor_ready_subset.csv
```

The dataset itself is not bundled in this code package unless explicitly added by the authors before public release.
